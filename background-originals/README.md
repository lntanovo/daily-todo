# 背景原图

把你选好的背景放在这个文件夹即可，可以使用中文文件名。

- JPG / PNG / WebP：静态图片可自动缩小、压缩后使用，原图不会被改写。
- GIF / 动态 WebP：需要保留动画，不能按静态图片压缩。
- MP4 / WebM：可以作为后续视频背景素材，当前上传入口暂不接收视频。

此目录只存原始素材，已加入 Git 忽略列表，不会自动上传到 GitHub 或公开网站。
网站精选图库在 `src/background-catalog.js` 配置；目前为空，等待你提供图片。
