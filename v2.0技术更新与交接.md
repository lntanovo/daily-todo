# 首页精简与专注功能技术更新交接（原 v2.0 方案）

更新日期：2026-09-22

状态：**阶段 A—D 已作为 v1.3.3 小版本实现；专注记录保持浏览器本地保存，本次不做云端迁移，也不发布 v2.0。**

本文把本轮产品需求、建议实现方式、验收条件以及下一位 Codex 必须知道的项目事实合并在一个文件中。它不是发布完成记录，不代表文中拟新增的文件、数据库表或功能已经存在。

## 1. 下一位 Codex 先看这里

- 继续使用原项目：`D:\codex\project\日程安排`。不要新建另一套工程，不要迁移到 C 盘。
- 当前发布目标：`v1.3.3`；不创建 v2.0 版本或标签。
- 分支：`main`；GitHub：<https://github.com/lntanovo/daily-todo>，仓库公开。
- 正式网站：<https://daily-todo-d3gq5mama7a468d02-1485775300.tcloudbaseapp.com/>。
- CloudBase 环境：`daily-todo-d3gq5mama7a468d02`，地域 `ap-shanghai`。
- 已有完整实现说明：[清水河最高机密.md](./清水河最高机密.md)。本文保留原方案文件名，仅作本次小版本的设计与交接记录。
- 本文创建前工作区干净；新对话必须重新检查 `git status` 和最新提交，不能假设环境未变化。
- 设置侧栏、农历日期条、可切换语录和本机专注计时已在源码与本地测试夹具中实现；专注记录暂按账号隔离保存在浏览器本机，没有执行云端迁移。
- 用户本次只要求编写更新与交接文档。下一位先读取本文，再按新对话中的实际指令开始开发。

### 本轮用户的最新要求

1. 添加能记录专注时间的番茄钟。
2. 顶部只保留「回到今天」「＋ 新建事项」两个主要按钮，其余操作集中到侧边设置。
3. 删除首页七天日期格、周日期范围和前后周箭头；所有日期切换收进现有日历按钮。
4. 首页日期信息简洁显示：今天的公历日期、农历、星期几。
5. 删除「今天，需要完成的清单」以及「这一天，需要完成的清单」这类大标题，改成可切换的中二、抽象或名言句子。
6. 继续简化界面；保留上传背景，文字直接显示在背景上，不要重新添加白色区域底板。
7. 番茄钟主控直接放在首页 `TO DO LIST` 右侧；设置中保留时长、任务关联与统计。
8. 设置齿轮入口固定在页面左下角，设置抽屉仍从右侧展开。
9. 品牌只显示大写 `TO DO LIST`，删除下方英文副标题。
10. 日期栏字号加大、提高醒目程度；语录相对缩小。
11. 句子除分类轮换外，提供大量内置选项、直接选择和用户自定义输入。

以上是产品方向。本文标为“建议”的时长范围、数据库字段、组件文件名等是交接用的设计建议，不应说成用户逐项确认过的要求。

## 2. 产品目标与首页结构

让首页集中展示日期、当天任务和一句有趣的话。设置和统计按需展开，减少同时暴露的按钮和说明文字。

```text
TO DO LIST       [FOCUS 25:00 · 开始]   [回到今天] [＋ 新建事项]

2026年9月22日 · 星期二 · 农历八月十二                   [日历图标]

「世界加载很慢，先完成这一件。」                       [换一句]
                                         小尺寸完成进度 2/5

任务标题
  当日补充 / 时间段 / 优先级 / 关联后的今日专注时间
──────────────────────────────────────────────────────────
其他任务……

左下固定[设置齿轮]                               [原有宠物]
```

上图只说明信息层级，不是固定像素设计。首屏高度应明显低于旧版的“周视图＋超大标题”，让任务更早进入视野。

### 必须保留的视觉约束

- 只有 Warm Paper 暖色方案，不恢复冷色主题。
- 品牌标题只保留大写 `TO DO LIST`，使用正体，不显示英文副标题。
- 任务标题继续用思源宋体，当日补充继续用霞鹜文楷。
- 上传背景继续使用 40% 的全页暖色遮罩，除非用户另行要求调整。
- 主页文字直接叠在背景上；禁止恢复之前被否决的白色品牌块、日期块、标题块和任务列表底板。
- 复杂背景上的文字保留深色字、细浅色字边和适度加粗，避免重新变得难以辨认。
- 设置抽屉、表单、日历等临时操作面板可以使用暖色实底；这不同于在主页内容背后添加永久白块。
- 中英混排，以中文为主；不新增说明性口号填满空白。

## 3. 设置侧栏 / SETTINGS

### 3.1 入口与展开方式

- 首页左下角保留一个固定齿轮按钮，附可访问名称「打开设置」；抽屉仍从右侧展开。
- 桌面点击后从右侧展开约 360–400px 宽的设置抽屉。
- 手机端宽度不得超出屏幕，按钮和可点击图标至少 44px。
- 支持关闭按钮、Esc 和点击遮罩关闭；关闭后焦点回到触发按钮。
- 抽屉打开时管理焦点和页面滚动。进入背景选择或专注记录等二级界面时，避免多个模态层同时抢焦点。
- 宠物可以被拖到任何位置，不能仅凭“默认在右下角”就假定它不会挡住齿轮；需要处理重叠和窄屏避让。

### 3.2 设置内容

| 分组 | 内容 |
| --- | --- |
| 账号与同步 | 当前账号、云端保存/失败状态、退出登录 |
| 专注 / FOCUS | 开始番茄钟、专注时长、休息时长、声音开关、今日与七天记录 |
| 外观与陪伴 | 背景选择、上传背景、恢复暖纸、花瓣动效开关、宠物显示开关 |
| 句子 / WORDS | 中二、抽象、名言、混合模式；当前句子的切换入口 |
| 数据 | 检测到旧本机数据时提供导入入口；无旧数据时隐藏 |

“云端保存”不再常驻顶部。保存失败仍须在主页给出清楚的提示，不能只有打开设置才知道数据没有保存。

### 3.3 现有代码接入注意

优先移动既有 DOM 控件，复用 ID 和事件，避免复制一套有重复 ID 的按钮：

- `saveMode`、`userIdentity`、`logoutButton` 移入账号区。
- `backgroundButton` 移入外观区，继续驱动 `setupAppearance()`。
- `motionButton` 移入陪伴区；注意它当前同时控制宠物和花瓣。
- `importButton` 及迁移提示需要统一归入数据区，保留真实的错误反馈。
- 顶部继续保留 `todayButton`、`addButton`。

宠物独立开关属于新增行为：当前 `setupCompanion()` 的 `enabled` 同时影响宠物、花瓣和点击交互。需要拆分宠物可见性与装饰动效状态，不能只移动按钮后就宣称已支持独立设置。保留 84px 宠物尺寸、拖动位置记忆、点击回应与减少动效偏好。

## 4. 日期区域与农历

### 4.1 首页必须删除的内容

- `weekLabel` 显示的整周范围。
- `days` 七天横向格子。
- `prevWeek`、`nextWeek` 两个首页翻周按钮。
- 与这些控件绑定的渲染、事件和“必须存在”的旧测试断言。

首页只留下精简日期信息和一个 `calendarButton`。年月选择、前后月份、选择具体日期、回到今天都在现有日历里完成。

### 4.2 “今天”与“正在查看的日期”要分清

用户要求首页标注今天，但网站必须继续支持查看别的日期。建议采用以下明确规则：

1. 顶部日期信息始终展示真实的今天、公历、农历和星期。
2. 默认任务列表显示今天。
3. 通过日历选择其他日期后，任务列表上方出现一行小字「正在查看：2026年10月1日 · 星期四」，不能继续把该列表叫作“今日任务”。
4. 返回今天后隐藏这行小提示，不形成重复日期区域。
5. 主按钮「回到今天」继续恢复今天的清单；它与日历内部的回到今天执行同一逻辑。
6. 新建任务仍默认绑定当前 `selectedDate`，表单必须清楚显示任务日期。

这一“非今日时才显示的小提示”是为避免日期混淆的设计建议。不能为了固定显示今天而悄悄删掉查看历史/未来任务的能力。

### 4.3 农历实现建议

- 优先使用浏览器内置的 `Intl.DateTimeFormat('zh-CN-u-ca-chinese', ...)`，不引入需要联网的农历接口。
- 已在本机 Node 环境验证：2026-09-22 中国时间的结果为八月十二，`formatToParts()` 的 `day` 返回数字字符串 `12`，需要映射为“十二”。
- 注意农历月名、闰月、正月、初一至三十的中文显示；不要把公历月份当农历月份。
- 检查 `resolvedOptions().calendar` 和目标浏览器表现；若不支持农历，保留公历和星期，不显示编造的农历。
- 建议以 `Asia/Shanghai` 统一今日日期、农历和专注按日统计。旧代码多处使用浏览器本地时区，实施时统一日期工具，不能只改其中一个标签。
- 在跨午夜和页面从后台恢复时刷新“今天”。用户若仍在跟随今日视图，则切到新一天；若在查看历史日期，则保持所选日期。
- `YYYY-MM-DD` 作为业务日期键，不直接用 UTC 的 `toISOString().slice(0,10)` 代替中国本地日期。

## 5. 可切换句子

### 5.1 行为

- 替换旧 `headline` 内容，当前字号约 25–40px，最多自然显示两到三行，长句不能挤压操作区。
- 句子旁提供轻量「换一句」按钮，不新增顶部大按钮。
- 支持中二、抽象、名言、混合四种模式；模式选择放设置里。
- 手动切换时尽量不重复当前句子；当前筛选只有一句时保持原句。
- 同一天刷新不应无故换句子；勾选任务、保存补充、切换日期等重绘不得随机换句。
- 建议每日首次进入时选一句，手动更换后记住当天选择；按用户 ID 隔离缓存。
- 不使用收费 AI 生成或外部语录接口。内置短句库即可满足首版需求。
- 内置句库不少于 30 句，在设置中提供完整下拉直选；允许用户输入最多 80 字的个人句子，并按账号保存在本机。

### 5.2 内容建议

以下是可采用的原创草稿，不冒充名人名言：

| 类型 | 示例 |
| --- | --- |
| 中二 | 今日的封印，由我亲手解除。 |
| 中二 | 命运的进度条，先推一格。 |
| 抽象 | 世界加载很慢，先完成这一件。 |
| 抽象 | 大脑已上线，拖延正在排队。 |
| 抽象 | 今天也在给人生修一点小 bug。 |

名言分类只加入出处已核实、长度适宜的句子，并记录作者和出处。未核实的网络句子归入原创/网络短句，不能随便署名给名人。具体句库不是用户最终选稿，开发预览时可以调整。

### 5.3 数据结构建议

新增静态 `quote-catalog.js`，每条包含 `id`、`category`、`text`、可选 `author`、`source`。用 `textContent` 渲染。语录偏好和当前选句与任务数据独立保存，不要写进任务标题或每日补充。

## 6. 番茄钟 / FOCUS

### 6.1 首版功能范围

- 开始、暂停、继续、结束和剩余时间直接位于首页品牌右侧；专注/休息时长、声音、任务关联和统计位于设置抽屉。
- 默认专注 25 分钟、休息 5 分钟，开始前可修改。
- 支持开始、暂停、继续、提前结束。
- 建议时长校验范围：专注 1–180 分钟、休息 1–60 分钟，整数分钟；这是实现建议，不是已有产品限制。
- 可选择关联一个任务，也可以不关联。
- 完成或提前结束后记录实际计时时长；暂停时间与休息时间不计入专注总时长。
- 专注结束后由用户选择是否开始休息，休息结束后由用户开始下一轮，不自动无限循环。
- 默认提供页面内提示；声音设置放在侧栏。声音只能在获得浏览器用户手势后播放，并处理被浏览器阻止的情况。
- 首版不做系统通知、短信提醒或推送；完全关闭浏览器时不承诺响铃。

### 6.2 收起后的计时器

- 进行中或暂停时，在左下角显示小型计时器，包含阶段、剩余时间、暂停/继续和展开入口。
- 尚未开始时不显示悬浮计时器；结束后提示可关闭，不能永久占位。
- 在手机端和宠物拖动后，不能遮住任务操作、设置按钮或表单提交按钮。
- 倒计时不能每秒通过 `aria-live` 打断屏幕阅读器，只播报关键状态变化。

### 6.3 统计规则

- 保存秒级数据，展示时转为分钟或“分秒”。不足一分钟可以显示“不到 1 分钟”，不得一律向上取整。
- 今日累计时长包括正常完成和提前结束的有效专注时间。
- 今日完整轮数只统计完成整个专注目标的轮次，休息不计轮数。
- 展示最近七天每日专注时长和明细：开始时间、实际时长、状态、关联任务。
- 同一任务可以关联多轮；任务下方显示所查看日期的关联专注总时长，不能把今天的统计套到历史任务上。
- 番茄钟结束不自动勾选任务完成。
- 跨午夜的一轮，时长按各日期实际占用区间拆分；完整轮数建议计入完成所在日期。
- 删除任务后保留历史专注记录与任务标题快照，不能级联删掉整段学习记录。

例：完成一轮 25 分钟，再进行 18 分钟提前结束，结果为 43 分钟专注、1 个完整轮次。

### 6.4 刷新、后台、睡眠与多设备

- 时间依据时间戳计算，不能靠每秒 `remaining--` 累计；浏览器后台会降低定时器频率。
- 暂停时锁定已专注秒数；恢复时重新计算结束时间，暂停期间不计时。
- 离开页面、电脑睡眠后再回来，最多计到本轮预定结束时刻，不能把离开几小时都算成专注。
- 这记录的是用户开启的计时区间，不检测用户是否真的在学习。
- 同账号建议只允许一轮未结束的专注/休息计时；双标签页和多设备不能各自启动后重复累计。
- 同浏览器可以用 BroadcastChannel 辅助同步，但真正的唯一性必须由云端约束保证。
- 关闭页面不能依赖 `beforeunload` 必然保存成功。会话在开始和状态切换时持久化，重新打开时恢复并校正。
- 退出登录前尝试暂停当前计时并保存；退出后停止本地更新、声音和统计展示，清理账号内存缓存。
- 无网络时不假装“已保存”。建议首版新一轮需要联网创建，已有轮次可继续显示本地计时；失败的状态变更保留待同步信息并防重复重试。离线编辑与跨设备冲突需要显示清楚的恢复结果，不能静默覆盖云端新状态。

## 7. 现有工程事实：避免下一位走错方向

| 项目 | 当前实现 |
| --- | --- |
| 前端 | 原生 HTML/CSS/JavaScript ES Modules，没有 React/Vue |
| 构建 | Vite 7；`package.json` 当前版本 1.3.3 |
| 主应用 | `index.html` 同时包含布局、主要 CSS、登录和任务状态/读写逻辑 |
| 身份 | CloudBase Auth 用户名密码；真实会话用 `auth.getSession()` 判断 |
| 数据库 | CloudBase PostgreSQL；浏览器通过 `cloudApp.rdb()` 访问 |
| 背景文件 | `cloudApp.storage.from('todo-backgrounds')`，私有存储、用户路径与签名 URL |
| 注册 | `src/registration.js` + `cloudfunctions/register-friend`，不走短信 |
| 注册限额 | 已记录的生产设置为每日 20、累计 100；本轮不调整 |
| 用户隔离 | `owner_id TEXT DEFAULT auth.uid()` 与 PostgreSQL RLS |
| 依赖 | `@cloudbase/js-sdk`；开发依赖 Vite |
| 字体 | `assets/fonts/` 自托管，部署需保留许可证 |

### 7.1 已有数据表

- `todo_tasks`：任务、单日/日期范围、优先级、每日起止时间。
- `todo_daily_completions`：按用户、任务、日期独立保存完成状态。
- `todo_daily_notes`：按用户、任务、日期保存最多 500 字补充。
- `todo_preferences`：当前只包含背景类型、背景值等背景设置。
- `friend_registrations`、`friend_registration_settings`：服务端私有注册台账和限额。

切勿使用 MySQL、NoSQL 集合、旧 `app.database()` 或旧版存储 SDK 代替现有 PG 接口。

### 7.2 需要重点修改的现有位置

| 文件/符号 | v2.0 接入点或陷阱 |
| --- | --- |
| `index.html` 顶栏 | 移动按钮到侧栏，保留 ID，避免重新绑定重复事件 |
| `state.selectedDate` | 继续作为任务列表所选日期；独立于“真实今天”和番茄钟当前时间 |
| `state.weekAnchor`、`renderWeek()`、`moveWeek()` | 周视图删除后清理依赖，不能只删 HTML 导致空节点报错 |
| `render()` | 当前每次都会调用 `renderWeek()`；需更新为新的日期/列表渲染流程 |
| `renderTasks()` | 当前每次重绘都会覆盖 `fullDate` 和 `headline`；语录必须移出这个覆盖逻辑 |
| 新建任务保存回调 | 当前会更新 `weekAnchor`；周视图移除后同步清理 |
| `showLogin()` / `enterApp()` | 新增设置、专注、语录模块的加载与退出重置；防止跨账号迟到请求串数据 |
| `setAppBusy()` | 现有任务操作使用共享 busy；不要让每秒计时刷新把整个应用锁住 |
| `src/calendar.js` | 已支持年月、月切换、键盘、任务点和回到今天，优先复用 |
| `src/appearance.js` | 背景上传和签名 URL 刷新已存在；移入侧栏时保留生命周期 |
| `src/companion.js` | `setupCompanion({motionButton})`；当前没有独立宠物显示开关 |
| `src/daily-notes.js` | `setupDailyNotes()` 具有按日期缓存和 generation 隔离机制，可参考其账号重置做法 |
| `src/features.css` | 背景透明区和细字边已在此实现；更新新组件选择器时保留这些效果 |

### 7.3 当前界面参数

- 页面基础纸色 `#f3ebdd`；图片背景遮罩 40%。
- 图片背景下文字 `--ink: #151310`、辅助文字 `#29241f`。
- 图片背景下强调色 `#872b17`、普通优先级 `#174d75`、紧急 `#a12720`。
- 细字边为八向 1px、零模糊的浅色 text-shadow；小字 13px、600 字重。
- 实色主要按钮和选中日期不描边；弹窗文字不应被主页字边规则污染。
- 这些是用户几轮反馈后确认并上线的结果，不要未经说明恢复白色底板或高灰度遮罩。

## 8. v2.0 建议模块划分

以下是拟新增文件，当前不存在：

| 拟新增文件 | 职责 |
| --- | --- |
| `src/settings.js` / `settings.css` | 抽屉、分组、焦点、现有控件接入 |
| `src/date-display.js` | 今天、公历、农历、星期、跨午夜与非今日提示 |
| `src/quote-catalog.js` | 内置短句数据和出处 |
| `src/quotes.js` | 选句、换句、筛选和持久化 |
| `src/focus-core.js` | 纯计时计算、暂停区间、跨日分摊，不依赖 DOM |
| `src/focus-store.js` | 云端会话、统计读取、版本冲突与重试 |
| `src/focus.js` / `focus.css` | 计时面板、迷你计时器和记录展示 |
| `tests/focus.test.mjs` | 时间边界、暂停、跨日和恢复的纯逻辑测试 |
| `tests/v2-ui.test.mjs` | 设置、日期、语录和番茄钟的浏览器验收 |
| `cloudbase/migrations/<新时间戳>_focus_and_settings.sql` | 新表、字段、RPC、约束和 RLS，开发时再生成 |

文件名可根据实现调整，但不要把所有新增功能继续堆进已有的大型 `index.html`，也不要为了这些功能重写整个项目框架。

## 9. 云端数据设计建议：尚未创建

### 9.1 会话表 `todo_focus_sessions`

建议字段：

| 字段 | 用途 |
| --- | --- |
| `owner_id` / `id` | 用户与会话 ID，组合主键；owner 为 TEXT，默认 `auth.uid()` |
| `phase` | `focus` 或 `break`；休息不参与专注统计 |
| `status` | `running`、`paused`、`completed`、`ended` |
| `task_id` | 可空的任务关联，必须属于同一用户 |
| `task_title_snapshot` | 保存启动时任务标题，删除任务后历史仍可读 |
| `planned_seconds` | 本轮目标时长 |
| `elapsed_seconds` | 已确认的有效计时秒数，排除暂停 |
| `started_at` / `running_since` / `deadline_at` | 首次开始、当前运行段起点、预定结束时间 |
| `ended_at` / `updated_at` | 实际结束与更新时间 |
| `version` | 乐观锁版本，处理并发状态切换 |

### 9.2 时间段表 `todo_focus_segments`

记录每个实际运行区间，建议包含 `owner_id`、`session_id`、段序号、`started_at`、`ended_at`。暂停时关闭当前段，继续时新开一段。它用于准确处理跨午夜与多次暂停，不能简单把总时长全归到开始日期。

建议约束：

- 一个账号至多一轮 `running` 或 `paused` 会话，使用数据库唯一约束兜底。
- 一轮至多一个未关闭的运行段；开始/暂停/恢复/结束原子更新会话和运行段。
- 已计秒数非负且不超过本轮计划；时间段截止不得越过本轮 deadline。
- 任务关联检查包含 owner；删除任务应解除关联、保留标题快照，不级联删除专注记录。

### 9.3 原子状态变化与幂等

推荐通过登录用户可调用的 PG RPC 处理开始、暂停、继续、结束和到期恢复，而不是让浏览器分别修改多张表。

- 开始时用客户端生成且重试不变的会话 ID；同 ID 同请求不创建第二轮。
- 状态修改携带 `expected_version`，数据库锁定会话后校验版本和当前状态。
- 重复结束应返回已完成结果，不累计第二次；旧版本命令返回最新状态供界面同步，不覆盖新状态。
- RPC 返回数据库时间，用于客户端校准；倒计时只负责显示，不每秒写云数据库。
- 页面重新出现时如果已到 deadline，按照 deadline 结算，不以重新打开网页的时刻结算。
- 定时任务不是首版必需：到期结果可以在下一次读取/同步时幂等结算，统计查询需纳入已到期尚未结算的情况。

实施前检查实际 SDK 的 `db.rpc()` 和 CloudBase PG 权限能力。若用受控的 `SECURITY DEFINER` 函数，需要固定 search_path、校验 `auth.uid()`、撤销 PUBLIC/anon 的执行权限，只授权需要的 authenticated 入口；不能让它成为绕过用户隔离的通用 SQL 通道。保持新表 RLS，按最小权限开放读取和受控写入。

### 9.4 设置存储

建议为 `todo_preferences` 增加专注时长、休息时长、声音、语录分类、宠物显示、动效开关等明确字段，并提供兼容旧用户的默认值。

注意 `src/appearance.js` 当前仅 upsert 背景字段。新增共享偏好后，各模块只能 patch 自己拥有的字段，不能用旧缓存整行覆盖，避免改背景时把番茄钟或语录设置重置。

宠物位置继续保存在当前浏览器；当前选句可用按 UID 与日期分隔的本机缓存。专注记录、用户设置的云端同步与 UI 临时开关状态要明确区分。

### 9.5 权限与迁移要求

- 所有用户表使用 `owner_id TEXT` 和 `auth.uid()`；普通账号只能访问自己的记录。
- 不改注册后端，不读取朋友关系资料，不新增短信费用。
- 新迁移放入现有 `cloudbase/migrations/`，不要修改已执行的历史迁移。
- 先检查云端实际 schema 与 migration history，再生成时间戳和执行迁移。
- 迁移用 `managePgDatabase(action='applyMigration')` 等版本化途径；不能把管理密钥放进浏览器执行建表。
- 先用假后端完成可审阅的本地预览。生产数据库迁移及网站发布，按用户后续明确授权执行；“之前允许 v1.3.2 上线”不等于 v2.0 已获上线许可。
- 如需新包、新插件、额外付费服务或账号，按用户原有约定单独说明必要性并取得允许。内置农历与语录原则上不需要新服务。

## 10. 开发环境、测试和发布事实

### 10.1 本地开发

```powershell
Set-Location 'D:\codex\project\日程安排'
npm run dev -- --port 5193 --strictPort
```

本次对话的预览地址为：<http://127.0.0.1:5193/tests/features-fixture.html>。

新对话不能假设旧服务仍在。先检查端口和工作目录，避免重复占用。用户指定浏览器操作时偏好电脑安装的 Edge，不要擅自用 Codex 右侧内置浏览器替代。

### 10.2 假后端与真实云端

- `vite.config.js` 仅在开发模式动态提供 `/tests/features-fixture.html`。
- 夹具把 CloudBase SDK 替换成 `tests/fake-cloudbase.js`，使用示例任务和浏览器本地数据，顶部有黄色预览提示。
- 当前假后端只有已有表、简单 CRUD、假会话和假存储，没有真实事务/RPC；增加番茄钟后要明确扩展模拟能力。
- 假后端测试通过不代表真实云端 RLS、并发锁或跨设备恢复已验证。
- `.env.local` 已有真实前端配置且被 Git 忽略；不要打印其值或写进交接文档。
- CloudBase 默认域名可能出现风险提示，先通过提示再检查登录页，不能把提示误判成项目故障。

### 10.3 现有检查命令

```powershell
npm run check
npm run test:registration
npm run test:features
npm run build
```

浏览器测试当前使用本机 Edge 和已有 Playwright。默认 Playwright 模块路径写在 `tests/features.test.mjs` 中，也支持 `PLAYWRIGHT_MODULE` 指定；另一台电脑未必存在该路径，不能未经允许自动安装依赖。

### 10.4 测试更新陷阱

- `tests/project-check.mjs` 当前要求 `id="days"` 存在，周视图删除后必须根据新产品要求更新检查，不能留一个无意义隐藏格子骗过测试。
- `tests/features.test.mjs` 当前直接点击页头 `backgroundButton` 和 `logoutButton`；搬进抽屉后先打开设置，再操作对应按钮。
- 原有按日补充、GIF/WebP、字体、失败重试、退出清理和窄屏检查必须继续保留。
- 需要更新日期/标题相关旧断言，不应继续依赖“今天，需要完成的清单”。

### 10.5 正式发布方式

现有站点通过 CloudBase 静态托管发布，继续使用既有域名与路径，不另建 Sites 或新的应用域名。

上一版流程为：验证 `auth(status)` 绑定正确环境 → 查询 hosting 配置 → 构建 → 上传 `dist/assets` 和 `dist/licenses` → 校验新资源 → 最后上传 `dist/index.html` → 核对首页和错误页均为 `index.html` → 公网验证 → Git 提交、推送与版本标签。

当前 MCP 名称为 `mcp__cloudbase_mcp__auth`、`queryHosting`、`manageHosting` 等；实际名称和 schema 以新会话工具发现为准。不可猜参数。

v2.0 如涉及数据库，必须先验证向后兼容迁移，再发布依赖新表的前端。不能仅上传页面就宣称番茄钟云端保存完成。

GitHub 网络曾使用本机代理 `http://127.0.0.1:7897`，新对话应先检查它是否仍可用。不要更改全局代理配置或把凭据写进仓库。

## 11. 实施顺序与任务清单

阶段 A—D 已于 2026-09-22 在本地源码中完成并通过自动验收。阶段 E 起仍需按后续授权推进。

- [x] **阶段 A：核对基线与本地 UI 草稿。** 已核对 `0329a00e8d488c7c7678289079b5dae448f17abc`、工作区与 v1.3.2 文档；完成紧凑首页、品牌右侧番茄钟和左下设置入口。
- [x] **阶段 B：设置和日期。** 已移动现有按钮、删除周条及事件、复用日历、增加上海时区公历/农历/星期和非今日提示，并拆分宠物/动效开关。
- [x] **阶段 C：可切换句子。** 已建立 30 多句内置句库、四种类别、下拉直选、用户自定义、手动切换及日期/账号隔离缓存；任务重绘不再覆盖句子。
- [x] **阶段 D：番茄钟本地闭环。** 已实现纯时间逻辑、暂停恢复、提前结束、手动休息、迷你计时器、最近七天统计、任务关联与本机持久化；可控时钟测试覆盖跨午夜与暂停区间。
- [ ] **阶段 E：云端数据实现。** 检查 schema；编写新迁移与受控 RPC；完善 RLS、幂等、单活动会话、跨日统计；授权后应用云端迁移。
- [ ] **阶段 F：完整验收。** 本地交互回归、真实账号云端记录和隔离、多标签页/多设备、断网恢复、字体和背景可读性。
- [x] **阶段 G：用户确认上线。** 用户确认按小版本上传；发布号定为 v1.3.3，不创建 v2.0 标签，并同步 GitHub 与文档。

本轮明确不把 `package.json` 修改为 2.0.0，也不创建 v2 标签。

## 12. 验收标准

| 编号 | 场景 | 应达到的结果 |
| --- | --- | --- |
| UI-01 | 打开首页 | 顶部只有回到今天与新建事项两个主要操作，其他入口位于侧栏 |
| UI-02 | 有个人背景 | 无永久白色区域块，正文和小字仍可辨认，大写正体 TO DO LIST 保留 |
| UI-03 | 桌面/手机打开设置 | 焦点正确，能关闭，无溢出，不被宠物遮挡 |
| DATE-01 | 今日视图 | 只显示紧凑公历/农历/星期和日历图标，没有七天条和前后周按钮 |
| DATE-02 | 日历选未来或历史日期 | 加载正确任务、补充及进度，并显示正在查看的日期 |
| DATE-03 | 跨年、闰日、农历闰月、午夜 | 日期计算正确，显示今天和所选日期不混淆 |
| QUOTE-01 | 换一句/换类型 | 句子正确切换，长句不溢出，无伪造名人署名 |
| QUOTE-02 | 刷新、勾任务、切日期 | 当前句子按既定规则保持，不随每次 render 随机变化 |
| FOCUS-01 | 专注 25 分钟完成 | 记录 1500 秒专注和一轮完成，休息等待用户开启 |
| FOCUS-02 | 专注 10 分钟，暂停 5 分钟，再专注 8 分钟提前结束 | 记录 18 分钟，不是 23 分钟，不增加完整轮数 |
| FOCUS-03 | 提前结束不足一分钟 | 保存实际秒数，展示准确，不虚增一分钟 |
| FOCUS-04 | 刷新、后台、睡眠后回来 | 恢复同一会话，最多计到 deadline，不重复结算 |
| FOCUS-05 | 双标签页/多设备同时开始或结束 | 同账号只有一个活动会话，不重复记录或覆盖新状态 |
| FOCUS-06 | 23:50 到次日 00:15 连续专注 | 前日 10 分钟、次日 15 分钟，总计 25 分钟，完成轮数归次日 |
| FOCUS-07 | 休息、任务关联、任务删除 | 休息不计专注；任务完成不被自动勾选；删除任务保留历史 |
| FOCUS-08 | 断网/保存失败/重试 | 明确显示待同步或失败，保留可恢复状态，恢复后不重复计数 |
| AUTH-01 | A 退出后 B 登录 | B 看不到 A 的专注、偏好、补充或背景，迟到请求不串号 |
| AUTH-02 | 匿名或另一账号直接访问新表/RPC | 被权限规则拒绝；管理端查询成功不能替代此项 |
| REG-01 | 原有功能回归 | 登录注册、任务 CRUD、单日/连续任务、优先级、时间、补充、上传背景、宠物均正常 |
| RELEASE-01 | 构建和公网 | 不发布 fake-cloudbase 或预览数据；线上资源匹配构建；版本、Git、文档一致 |

布局至少检查 320、390、720、721、1024、1440px，包含长句、长任务、深色和高纹理背景。跨设备/RLS 没有真实账号条件时，应明确标成未验证，不可写“全部通过”。

## 13. 给新对话的启动消息

把下面这一段发给下一位 Codex，并在同一个项目目录中开始：

> 继续开发 D:\codex\project\日程安排。请先完整阅读项目根目录的《v2.0技术更新与交接.md》，再参考《清水河最高机密.md》和 README.md，并检查 Git 状态。当前正式版是 v1.3.2，v2.0 还没有实现。请按交接文档推进设置侧栏、精简日期与农历、可切换句子和番茄钟，先做可交互的本地预览；不要恢复主页白色底板、七天日期条和斜体标题。需要变更云端数据库或发布正式网站时先说明具体变更，等待我确认。不要重复询问文档里已经明确的界面要求。

## 14. 当前完成范围

- 已核对 v1.3.2 基线，并将本次补丁版本定为 v1.3.3，没有创建 v2 标签。
- 已完成紧凑首页、品牌右侧番茄钟、左下设置入口、设置抽屉、醒目日期、农历与非今日提示、30 多句可直选/自定义语录、宠物/花瓣独立开关。
- 已完成本机番茄钟闭环：开始、暂停、继续、提前结束、手动休息、时间戳恢复、跨午夜分摊、七天统计、任务关联、任务标题快照和按账号隔离的浏览器持久化。
- 已新增 `focus-core` 单元测试并更新 Edge 浏览器功能验收；`npm run check`、`npm run test:focus`、`npm run test:features` 与 `npm run build` 均通过。
- 尚未新增或执行专注记录的云端迁移，尚未完成真实多设备唯一性、断网同步与 RLS 验收；本轮只发布 v1.3.3 静态前端，不发布 v2.0。

后续接手者完成任何阶段后，应更新第 11 节勾选状态及第 14 节事实，避免再把“计划”当作“已经完成”。
